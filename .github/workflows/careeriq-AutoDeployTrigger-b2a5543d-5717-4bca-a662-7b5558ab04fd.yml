name: Trigger auto deployment for careeriq

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/careeriq-AutoDeployTrigger-b2a5543d-5717-4bca-a662-7b5558ab04fd.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.CAREERIQ_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CAREERIQ_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CAREERIQ_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: 
          registryUsername: ${{ secrets.CAREERIQ_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CAREERIQ_REGISTRY_PASSWORD }}
          containerAppName: careeriq
          resourceGroup: rg-dshaik1-1365
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


